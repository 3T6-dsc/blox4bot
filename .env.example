# The token for your Discord bot.
DISCORD_BOT_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE

# The base URL for the backend API that the bot will communicate with.
BACKEND_API_URL=http://localhost:3000/api/v1

# The port the backend server will run on (optional, defaults to 3000).
PORT=3000
