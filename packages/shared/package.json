{
  "name": "@blox4bot/shared",
  "version": "1.0.0",
  "description": "Shared utilities and types for blox4bot",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@blox4bot/config": "workspace:*",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}
